<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zahir.flickrgalleryapplication">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlickrGalleryApplication">
        <activity android:name="com.zahir.flickrgalleryapplication.ui.details.DetailsActivity" />
        <activity android:name="com.zahir.flickrgalleryapplication.ui.gallery.GalleryActivity" />
        <activity android:name="com.zahir.flickrgalleryapplication.ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>